spring:
  application:
    name: service-zuul
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: service-config
eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka/
